{
  "name": "schedule-app",
  "private": true,
  "version": "0.1.0",
  "description": "Open-source, end-to-end encrypted, self-hostable scheduling application",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "postinstall": "test -d packages/shared/src && npm run build --workspace=@schedule-app/shared --workspace=@schedule-app/crypto || true",
    "build:libs": "npm run build --workspace=@schedule-app/shared --workspace=@schedule-app/crypto",
    "pretest": "npm run build:libs",
    "test": "npm run test --workspaces --if-present",
    "pretypecheck": "npm run build:libs",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "AGPL-3.0-or-later"
}
